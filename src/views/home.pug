extends base
include mixins/stamp-block

block content
    if loggedIn
        if loggedUser.email
            h1 home 교사 초기 화면  
            span 
                a(href="/setting")  칭찬 도장 및 학생 추가하기
            hr
        else 
            h2 학생 초기 화면 
    else 
        h2 로그인 해주세요. 

block scripts
    script(src="/static/js/board.js")