header 
    a(href="/").header__logo
        h1 🧞‍♂️ Genie Stamp
    //- if loggedIn
    //-     if  loggedUser.email
    //-         h2.header__greeting #{loggedUser.name} 선생님 환영합니다.
    //-     else 
    //-         h2.header__greeting #{loggedUser.name} 학생 환영합니다.
    nav.navbar 
        ul 
            if loggedIn
                if loggedUser.email
                    li 
                        a(href="/board")
                            i.fa-solid.fa-stamp
                            span  칭찬도장판
                    li 
                        a(href="/setting") 
                            i.fa-solid.fa-gear
                            span  학생 및 도장 관리        
                    li 
                        a(href="/stats")
                            i.fa-solid.fa-filter
                            span  통계
                    li
                        a(href="/logout") 
                            i.fa-solid.fa-right-from-bracket
                            span  로그아웃
                else
                    li 
                        a(href="/student-main") 홈 
                    li 
                        a(href="/student/password-change") 비밀번호 바꾸기
                    li 
                        a(href="/logout") 로그아웃
            else
                li 
                    a(href="/join") 회원가입
                li 
                    a(href="/login") 교사용 로그인
                li 
                    a(href="/login2") 학생용 로그인